# Notes
1. You have to use getters and setters for model objects to allow freemarker template to access object fields
2. The default freemarker template extension for spring boot is "ftlh" and the default location for templates is ```src/resources/templates```